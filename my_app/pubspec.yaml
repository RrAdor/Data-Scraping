name: sentimentscope
description: A sentiment analysis app for news articles

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0  # Alternative to charts_flutter
  syncfusion_flutter_charts: ^25.1.43  # Another alternative

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true